import { CategoryAndParentCategoryPipe } from './category-and-parent-category.pipe';

describe('CategoryAndParentCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new CategoryAndParentCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});

import { GetSalePricePipe } from './get-sale-price.pipe';

describe('GetSalePricePipe', () => {
  it('create an instance', () => {
    const pipe = new GetSalePricePipe();
    expect(pipe).toBeTruthy();
  });
});

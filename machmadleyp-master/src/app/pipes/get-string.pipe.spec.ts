import { GetStringPipe } from './get-string.pipe';

describe('GetStringPipe', () => {
  it('create an instance', () => {
    const pipe = new GetStringPipe();
    expect(pipe).toBeTruthy();
  });
});

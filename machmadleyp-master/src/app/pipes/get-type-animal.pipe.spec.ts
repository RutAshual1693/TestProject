import { GetTypeAnimalPipe } from './get-type-animal.pipe';

describe('GetTypeAnimalPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTypeAnimalPipe();
    expect(pipe).toBeTruthy();
  });
});

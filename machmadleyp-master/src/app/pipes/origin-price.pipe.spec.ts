import { OriginPricePipe } from './origin-price.pipe';

describe('OriginPricePipe', () => {
  it('create an instance', () => {
    const pipe = new OriginPricePipe();
    expect(pipe).toBeTruthy();
  });
});

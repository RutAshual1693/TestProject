import { DeleteOriginPricePipe } from './delete-origin-price.pipe';

describe('DeleteOriginPricePipe', () => {
  it('create an instance', () => {
    const pipe = new DeleteOriginPricePipe();
    expect(pipe).toBeTruthy();
  });
});

import { EmptyLinePipe } from './empty-line.pipe';

describe('EmptyLinePipe', () => {
  it('create an instance', () => {
    const pipe = new EmptyLinePipe();
    expect(pipe).toBeTruthy();
  });
});
